{
  "name": "amragrir",
  "private": true,
  "version": "0.0.0",
  "description": "Amragrir.am monorepo — api (NestJS), mobile (Expo), web (Next.js), admin (Vite+React).",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
