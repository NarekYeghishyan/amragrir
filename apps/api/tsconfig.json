{
  "extends": "../../packages/config/tsconfig.base.json",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "target": "ES2022",
    "outDir": "./dist",
    "baseUrl": "./",
    "declaration": false,
    "composite": false,
    // No `incremental`: combined with nest-cli's deleteOutDir it produces stale
    // builds — dist is wiped while the cache says files are unchanged, so they
    // are never re-emitted.
    "sourceMap": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "isolatedModules": false,
    "noUncheckedIndexedAccess": false
  },
  "include": ["src", "prisma"],
  "exclude": ["dist", "node_modules"]
}
