/**
 * The back office's component vocabulary.
 *
 * Screens import from here, never from `@radix-ui/*` directly — that keeps the
 * appearance in one place and means swapping a primitive is one file's work
 * rather than seven screens'.
 */
export * from './icons';
export * from './core';
export * from './controls';
export * from './overlays';
