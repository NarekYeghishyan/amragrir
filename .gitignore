# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.expo/
.expo-shared/
.turbo/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.history/

# Claude Code settings (harness/machine-specific — keep out of the shared repo)
.claude/

# Test / coverage
coverage/

# Expo
.expo/
expo-env.d.ts

# Runtime uploads — user-supplied images written by the API (UPLOAD_DIR).
# The repo ships its own artwork in apps/api/public, which is committed.
apps/api/uploads/
