packages:
  - "apps/*"
  - "packages/*"

# Allow these dependencies to run their install scripts (pnpm blocks them by
# default). Prisma needs its postinstall to fetch the query engine and generate
# the client.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  prisma: true
